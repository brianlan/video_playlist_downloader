# Implementation Plan: Video Playlist CLI Downloader

**Branch**: `001-playlist-downloader-cli` | **Date**: 2025-10-29 | **Spec**: specs/001-playlist-downloader-cli/spec.md  
**Input**: Feature specification from `/specs/001-playlist-downloader-cli/spec.md`

## Summary

Deliver a Python-based CLI that ingests a playlist URL, enumerates every accessible video via yt-dlp, throttles downloads to respect platform limits, persists metadata (and subtitles when available) into a local database, and provides resumable progress tracking so long-running batches complete reliably.

## Technical Context

**Language/Version**: Python 3.10 (conda environment `/ssd4/envs/llm_py310_torch271_cu128`)  
**Primary Dependencies**: yt-dlp, Typer (CLI scaffolding), SQLAlchemy, SQLite, Tenacity (retry control), Rich (console reporting)  
**Storage**: SQLite file (`video-storage/state.db`) accessed via SQLAlchemy ORM  
**Testing**: pytest with pytest-mock for dependency isolation, coverage.py for test completeness  
**Target Platform**: Linux CLI environments managed through the shared conda environment  
**Project Type**: Single-project CLI with supporting libraries under `src/video_playlist_downloader`  
**Performance Goals**: Sustain sequential downloads at throttled 2–3 concurrent tasks; maintain <5% re-download rate on resumes; CLI commands complete status reporting in under 2 seconds for 500-item playlists  
**Constraints**: Must enforce configurable rate limits to prevent IP bans; operations must be runnable offline after playlist metadata capture; all automation executed via documented conda activation command  
**Scale/Scope**: Designed for playlists containing up to 500 entries (≈200 GB total media); supports growth to tens of thousands of metadata records without schema changes

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- [x] Readability: Partition logic into explicit modules (`cli.py`, `downloader.py`, `persistence.py`, `throttle.py`) and favor declarative configuration objects over dynamic reflection so contributors can follow control flow without indirection.
- [x] TDD: Author failing tests first—`tests/cli/test_playlist_download.py`, `tests/persistence/test_resume_progress.py`, `tests/db/test_video_recording.py`, `tests/rate_limit/test_throttle_controls.py`—mocking yt-dlp invocations with fixtures and stubbing filesystem/database interactions before implementation.
- [x] Automation: Provide `make quality` (aliasing `conda activate /ssd4/envs/llm_py310_torch271_cu128 && pytest && ruff check`) plus a GitHub Actions workflow to run linting, formatting, and tests on every push and pull request.
- [x] Simplicity: Keep the solution as a single CLI application with SQLite persistence; defer service orchestration, distributed download workers, or message queues until proven necessary.
- [x] Rationale Capture: Record trade-offs and deviations in `docs/decisions/` ADRs; annotate complex throttling logic with intent-focused comments, and reference spec/clarifications within docstrings when behavior diverges from defaults.

## Project Structure

### Documentation (this feature)

```text
specs/001-playlist-downloader-cli/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── openapi.yaml
└── tasks.md                # generated by /speckit.tasks
```

### Source Code (repository root)

```text
src/
├── video_playlist_downloader/
│   ├── __init__.py
│   ├── cli.py
│   ├── config.py
│   ├── downloader.py
│   ├── throttle.py
│   ├── persistence.py
│   ├── metadata.py
│   └── subtitles.py

tests/
├── cli/
│   └── test_playlist_download.py
├── reporting/
│   └── test_subtitle_coverage.py
├── unit/
│   ├── test_subtitles.py
│   └── test_throttle_cli.py
├── integration/
│   └── test_cli_download.py
├── rate_limit/
│   ├── test_throttle_controls.py
│   └── test_throttle_metrics.py
├── perf/
│   └── test_metadata_latency.py
└── contract/
    └── test_contract_alignment.py

scripts/
└── run_quality.sh

reports/
├── throttle-metrics.md
└── subtitle-metrics.json
```

**Structure Decision**: Single Python package keeps CLI orchestration and domain logic co-located for readability; tests mirror module layout to reinforce TDD workflow; automation scripts reside in `scripts/` to centralize reusable commands.

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| _None_ | – | – |

### Constitution Check (Post-Design Review)

- [x] Readability: Research-backed module split and updated data model keep responsibilities explicit with no hidden coupling introduced during design.
- [x] TDD: Generated research, contracts, and data model confirm the four failing tests remain the first artifacts to implement and rely solely on mocks/fixtures.
- [x] Automation: `make quality` command and planned GitHub Actions workflow cover linting, formatting, and tests using the conda environment.
- [x] Simplicity: Design artifacts reaffirm a single-process CLI with SQLite storage; no additional services or queues were added.
- [x] Rationale Capture: Decisions documented in `research.md` and future ADRs ensure intent is traceable alongside implementation.
